[workspace]
members = [
    "router",
    "memory",
    "security",
]
resolver = "2"

[workspace.package]
version = "1.3.0"
edition = "2021"
authors = ["APEX Team"]
license = "MIT"

# Performance optimizations
[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
