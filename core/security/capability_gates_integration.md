# Capability Gates Integration (MVP)

- Concept: simple gating by permission tier for key actions (T0-T3).
- MVP: a tiny gate function that validates an action against a required tier before execution.
- Integration points: Hands runner and orchestrator; gate checks before executing high-risk actions; UI display in logs.
